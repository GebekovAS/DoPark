// import {
//   SET_BOOKING_STAGE,
//   GET_LIST_MARKERS,
//   GET_MARKERS_SUCCESS,
//   GET_MARKERS_FAILED
// } from 'Constants/Redux';
const initialState = {
  sevice: 'good'
};

export default function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
